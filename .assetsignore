.git
.github
wrangler.jsonc
.assetsignore
worker.js

# Lo que el worker sirve es TODO el directorio, así que lo que no se excluya
# acá queda publicado en el sitio. `supabase/` es el punto de restauración de
# la base: las 184 migraciones son el modelo de permisos entero —las políticas
# de RLS, las funciones SECURITY DEFINER, qué comprueba cada una— y el
# inventario del esquema es su índice. Nada de eso lo pide ninguna página; lo
# único que conseguiría publicarlo es darle a cualquiera el mapa de por dónde
# buscarle la vuelta. No da ningún error tenerlo abierto: solo se nota el día
# que alguien lo usa.
supabase
RESTAURAR.md

# Los volcados de la base (herramientas/respaldo-datos.sh). Están en
# .gitignore, así que no deberían llegar nunca a un despliegue — pero el
# despliegue sube la carpeta de trabajo, no lo que hay en git, así que quien
# corra el respaldo y despliegue después publicaría cédulas y correos de
# menores de edad sin enterarse. Dos candados para el mismo descuido.
respaldos

# El video promocional se regenera con herramientas/video-promo.js
promo/

# Las notas del proyecto y las herramientas de desarrollo. CLAUDE.md y docs/
# (las decisiones de diseño, un archivo por tema) explican el modelo de
# permisos entero —por la misma razón que `supabase/`, no tiene por
# qué estar publicado— y los scripts de herramientas/ no los pide ninguna
# página. De esa carpeta SÍ hay que dejar herramientas/cursos/: su
# catalogo.json lo pide el armador de planes de clase, así que se excluye lo demás
# por partes en vez de la carpeta entera.
CLAUDE.md
docs
.mcp.json
node_modules
package.json
package-lock.json
herramientas/*.js
herramientas/*.py
herramientas/*.sh
herramientas/*.mts
herramientas/*.md
herramientas/*.html
herramientas/css
herramientas/guia
herramientas/lib
herramientas/material
herramientas/planes
herramientas/plantillas
herramientas/video

# El estado local de `wrangler dev`: no es del sitio.
.wrangler

# Las imágenes para redes sociales (herramientas/imagenes-redes.js): se suben
# a mano a Facebook e Instagram, ninguna página las pide.
img/redes
